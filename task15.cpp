#include <iostream>
using namespace std;
main()
{   int size;
    cout << "Enter size: ";
    cin >> size;
    int number[size];
    for(int x =0 ; x < size ; x=x+1)
    {   cout << " Enter number: ";
        cin >> number[size];
    }
    
}